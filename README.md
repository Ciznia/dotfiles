# Ciznia dotfiles

This repo will host my own system(s) configuration but feel free to edit / copy it if you want

---

This repo will be mostly based on two tools, the nix flake configuration and ansible for the thing that can't be commited flat / to prepare the host to recieve the config
for exemple I will use ansible to automate my ssh/gpg setup so this will be using the ansible vault since I won't commit the ssh key flat ...
