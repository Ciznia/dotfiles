[defaults]
inventory           = ansible/inventories/local/hosts.yml
roles_path          = ansible/roles

# Vault password lives at repo root, one line, chmod 600, git-ignored.
# Create it (`openssl rand -base64 24 > .vault_pass`) or comment this out
# and pass --ask-vault-pass instead.
vault_password_file = .vault_pass
